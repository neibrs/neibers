# README
config directory contents has conflict core profile standard.

most config yml files of neibrs was copied from profile standard.


They are copyed from standard profile install and optional files
which in current install and optional directory files